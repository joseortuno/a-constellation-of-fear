{
  "name": "mocomuseo",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "standard": "standard",
    "github": "vite build && cp -a dist/. docs/ && git add . && git commit -m 'publish new build' && git push"
  },
  "devDependencies": {
    "standard": "^16.0.4",
    "vite": "^2.7.0"
  },
  "eslintConfig": {
    "extends": [
      "./node_modules/standard/eslintrc.json"
    ]
  },
  "dependencies": {}
}
